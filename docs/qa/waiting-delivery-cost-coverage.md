# Acceptance coverage: waiting-delivery-cost

AC list from `docs/specifications/waiting-delivery-cost.md` (confirmed, AC1–AC8). Evidence pulled from the two open PRs — `ngolik/arrival-service#5` and `ngolik/expenses-service#2` — not just the QA case docs, since code now exists.

| AC | Covered by | Result | Gap |
| --- | --- | --- | --- |
| AC1 — a waiting record exists in `arrival-service` and a cost record exists in `expenses-service`, both referencing the delivery id (reworded via `story-refinement`, 2026-08-31) | Inspection of both diffs against the reworded AC text: `Arrival.isWaiting`/`remark` (arrival-service), `Expense.DeliveryID`/`UserID`/`Amount` (expenses-service), correlated by delivery id, no live call between them | **pass** | None — resolved. Originally flagged **conflict** here because AC1 still said "one record is created" while the shipped architecture is two correlated records; the wording was corrected in the specification to match the already-approved architecture (no behavior change, no re-test needed). |
| AC2 — missing amount rejected | `TestAddWaitingDeliveryCost/missing_amount_is_rejected` (`expenses-service/service/waiting_delivery_cost_test.go`), `TestAddWaitingDeliveryCostHandler` table case (`api/waiting_delivery_cost_test.go`) | **not-run** | Test code exists and asserts the right thing, but `go build`/`go test` have never been executed against this branch — no Go toolchain in the authoring or review environment (stated in both the PR and its review report). Code-reviewed by hand only. Must be run before this can be marked pass. |
| AC3 — anonymous/unattributed operator rejected | `TestAddWaitingDeliveryCost/missing_user_id_is_rejected` + `/unknown_user_is_rejected` + `/auth-service_call_failure_is_an_upstream_error` (service tests, fake validator); handler-level equivalents in `api/waiting_delivery_cost_test.go` | **not-run** | Same as AC2 — real Go test coverage exists for presence *and* unknown-user rejection, but unexecuted. Also: only the fake validator is exercised — no test proves the real `authclient.HTTPUserValidator` correctly talks to a live `auth-service` (the exact class of gap already flagged for the sibling `expense-submitted-by-user` story). |
| AC4 — missing delivery id rejected | `TestAddWaitingDeliveryCost/missing_delivery_id_is_rejected` (service), handler-level equivalent (api) | **not-run** | Same toolchain gap as AC2/AC3. |
| AC5 — optional remark ≤500 chars, omitted doesn't block | `PUT arrivals waiting with no body returns 200 with a null remark()`, `PUT arrivals waiting with a remark returns 200 with the updated arrival response DTO()` (`arrival-service/src/test/kotlin/.../ArrivalControllerTest.kt`) | **pass** | None. Verified via `build/test-results/test/TEST-...ArrivalControllerTest.xml` — real executed test, not just code review. |
| AC6 — remark >500 chars hard-rejected, not truncated | `PUT arrivals waiting with a remark over 500 characters returns 400 and does not update()` | **pass** | None. Same XML evidence; test name confirms it also asserts the service was *not* called (no partial update), matching "hard reject." |
| AC7 — retrieve as a single record for that delivery | Arrival half: `GET arrivals by id surfaces waiting state and remark once set()` — **pass** (XML-confirmed). Expenses half: `GetWaitingDeliveryCost`/`GetWaitingDeliveryCostHandler` tests exist, plus a new id-assertion in both the service- and handler-level happy-path tests (commit `d61a47c`) — **not-run**. | **partial** | The functional gap is **fixed**: `POST /expenses/rest/waiting-cost` now returns `WaitingDeliveryCostResponse{id, deliveryId, userId, amount}` — root cause was `AddWaitingDeliveryCost` taking `model.Expense` by value, so `repo.Create`'s id assignment never reached the caller; fixed to take `*model.Expense`. Remaining gap is only the toolchain one, same as AC2–4 — the fix itself has never been run through `go build`/`go test`. |
| AC8 — no list-all-waiting-deliveries endpoint | Manual/design inspection of both diffs: `arrival-service` routes are `GET`, `GET /{id}`, `POST`, `PUT /{id}/waiting` only; `expenses-service` routes are `/add`, `/all`, `/waiting-cost` (POST), `/waiting-cost/{id}` (GET) only — no list-filtered-by-waiting endpoint in either | **pass** | None — this is a negative assertion, confirmed by inspection rather than a test, consistent with how `TC-10` in the QA case doc was already scoped (manual/design-check). |

## Summary
- Covered: 4 / 8 fully (AC1, AC5, AC6, AC8), 1 / 8 partial (AC7), 3 / 8 not-run (AC2, AC3, AC4)
- Gaps:
  1. ~~AC1's text doesn't match the shipped architecture~~ — **resolved**, reworded via `story-refinement` (2026-08-31).
  2. **Blocking for sign-off**: `expenses-service` has zero executed test evidence — everything there is "written, reviewed by hand, never run." Run `go build ./... && go test ./...` before treating AC2/AC3/AC4/AC7(expenses half) as anything but not-run.
  3. ~~`expenses-service`'s create response omits the new record's `id`~~ — **fixed** (commit `d61a47c`, pushed to `ngolik/expenses-service#2`). Still blocked on gap 2 above for actual pass/fail evidence.
  4. No test exercises the *real* `authclient.HTTPUserValidator` against a live/stubbed `auth-service` HTTP server for the AC3 create-path — only `authclient/client_test.go` tests the client in isolation, and only fakes exercise it from the service/handler tests.
- Verdict: **gaps-remain** — the one remaining blocker is running the Go test suite for real; everything else found in this pass has been fixed or resolved.
