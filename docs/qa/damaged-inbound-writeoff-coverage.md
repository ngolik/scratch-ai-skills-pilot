# Acceptance coverage: damaged-inbound-writeoff

AC list from `docs/specifications/damaged-inbound-writeoff.md` (confirmed, AC1–AC9). Evidence pulled from the actual working-tree diffs in `arrival-service` and `expenses-service` (branch `feature/damaged-inbound-writeoff`, uncommitted) — not just the QA scope/cases docs, since code now exists.

| AC | Covered by | Result | Gap |
| --- | --- | --- | --- |
| AC1 — damage record (arrival-service) + write-off amount (expenses-service), correlated by delivery id, not one combined artefact | `Arrival.isDamaged`/`damageRemark` + `PUT /{id}/damaged` (arrival-service); `Expense.DeliveryID`/`Amount` + `POST /damage-writeoff` (expenses-service), correlated by delivery id value only, no live call | **partial** | arrival-service half is executed and passing (`ArrivalControllerTest`). expenses-service half is written but unexecuted (Go toolchain unavailable — see AC2–4/AC8). No cross-service integration test exists for the correlation itself, matching how `waiting-delivery-cost`'s equivalent AC1 evidence was scored (diff inspection, not a live end-to-end test). |
| AC2 — missing write-off amount rejected | `service.AddDamageWriteOff` (`Amount == 0` → `ValidationError`); `TestAddDamageWriteOff/missing_amount_is_rejected`, `TestAddDamageWriteOffHandler` "missing amount - 400" | **not-run** | Test code exists and asserts the right thing (reviewed by hand), but `go build`/`go test` have never been executed on this branch — no Go toolchain on this machine. |
| AC3 — missing/unattributed operator rejected | `AddDamageWriteOff` (`UserID == 0` → `ValidationError`; unknown user via `validator.UserExists` → `ValidationError`); `TestAddDamageWriteOff/missing_user_id_is_rejected` + `/unknown_user_is_rejected`; handler-level equivalents | **not-run** | Same toolchain gap as AC2. Also: only the fake validator is exercised — no test hits a real/stubbed `auth-service`, same class of gap already flagged for `waiting-delivery-cost`. |
| AC4 — missing delivery id rejected | `AddDamageWriteOff` (`DeliveryID == 0` → `ValidationError`); `TestAddDamageWriteOff/missing_delivery_id_is_rejected`, handler-level equivalent | **not-run** | Same toolchain gap as AC2/AC3. |
| AC5 — optional remark, omitted doesn't block | `PUT arrivals damaged with a remark returns 200...()`, `PUT arrivals damaged with no body returns 200 with a null remark()` (`ArrivalControllerTest.kt`) | **pass** | None. Executed via `gradlew.bat test`, 17/17 passing. |
| AC6 — remark >500 chars hard-rejected, not truncated | `PUT arrivals damaged with a remark over 500 characters returns 400 and does not update()` — asserts `verify(arrivalService, never()).markAsDamaged(...)` | **pass** | None. Executed, confirms hard reject with no partial update. |
| AC7 — retrieve as a single record for that delivery | Arrival half: `GET arrivals by id surfaces damaged state and remark once set()` — **pass**, executed. Expenses half: `GetDamageWriteOffHandler`/`GetDamageWriteOff` tests exist, response includes `id`, `deliveryId`, `userId`, `amount` — **not-run**. | **partial** | Same expenses-service toolchain gap. |
| AC8 — write-off amount stays distinguishable from a `waiting-delivery-cost` amount for the same delivery | `Expense.IsDamageWriteOff` discriminator, set only by `AddDamageWriteOff`; `TestAC8_WaitCostAndDamageWriteOffAreDistinguishable` (service) and `TestAC8_HandlerLevel_...` (handler) — both create a wait-cost record and a damage-writeoff record for the *same* `DeliveryID` and assert the flag differs | **not-run** | Both dedicated AC8 tests live entirely in `expenses-service` — blocked on the same toolchain gap. This is the highest-value gap to close first, since AC8 was the story's central risk. |
| AC9 — no list-all-damaged-deliveries endpoint | Inspection of both diffs: `arrival-service` routes are `GET`, `GET /{id}`, `POST`, `PUT /{id}/waiting`, `PUT /{id}/damaged` only; `expenses-service` routes are `/add`, `/all`, `/waiting-cost`, `/waiting-cost/:id`, `/damage-writeoff`, `/damage-writeoff/:id`, `/:id` only — no list-filtered-by-damaged endpoint in either | **pass** | None — negative assertion, confirmed by inspection, consistent with how `waiting-delivery-cost`'s equivalent AC8 (its list-exclusion AC) was scored. |

## Summary
- Covered: 3 / 9 fully (AC5, AC6, AC9), 2 / 9 partial (AC1, AC7), 4 / 9 not-run (AC2, AC3, AC4, AC8)
- Gaps:
  1. **Blocking for sign-off**: `expenses-service` has zero executed test evidence — everything there is written and manually reviewed, never run. This is the identical class of gap `waiting-delivery-cost-coverage.md` flagged for the sibling story. Run `go build ./... && go test ./...` on a machine with Go before treating AC2/AC3/AC4/AC8/AC7(expenses half) as anything but not-run.
  2. AC8 in particular is the story's central risk (distinguishing damage write-offs from wait-cost amounts) — both of its dedicated cross-story tests are unexecuted; prioritize running these first if only a partial toolchain run is feasible.
  3. No live-`auth-service` integration test for the operator-identity check (AC3) — only fakes are exercised, matching the pre-existing gap noted for `waiting-delivery-cost`.
- Verdict: **gaps-remain** — the one blocker (execute the Go test suite) is identical to the sibling story's; everything found in this pass is either already covered on the `arrival-service` side or waiting on that same execution step.
