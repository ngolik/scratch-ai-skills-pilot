# Portfolio (local landscape)

Five sibling repos under `~/ptojects`. Canonical map: `services.yaml`.
Local paths: `services.local.yaml` (gitignored; copy from `.example`).

```powershell
$env:PRACTICE_HUB = "C:\Users\mikita.holik\ptojects\scratch-ai-skills-pilot"
```

Then in Claude Code:

```text
/demo-engineering:multi-repo-coordinator docs/specifications/<slug>.md
```
